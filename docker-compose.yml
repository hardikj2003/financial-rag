version: "3.9"

services:
  qdrant:
    image: qdrant/qdrant
    container_name: financial-rag-qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage: